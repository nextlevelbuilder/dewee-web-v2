# Checks every PR; deploys `dev` to staging.dewee.sh and `main` to dewee.sh + www.dewee.sh.
# Deploy steps run only when the CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID repo secrets exist,
# so forks and a fresh repo still get a green build.
name: CI / Deploy

on:
  pull_request:
    branches: [dev, main]
  push:
    branches: [dev, main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: ${{ github.event_name == 'push' && (github.ref_name == 'main' && 'production' || 'staging') || null }}
    env:
      # push to dev → staging config; everything else builds the production config.
      CLOUDFLARE_ENV: ${{ github.event_name == 'push' && github.ref_name == 'dev' && 'staging' || '' }}
      SITE_URL: ${{ github.event_name == 'push' && github.ref_name == 'dev' && 'https://staging.dewee.sh' || 'https://dewee.sh' }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      - run: pnpm check
      - run: pnpm build

      - name: Deploy gate
        id: gate
        if: github.event_name == 'push'
        run: |
          if [ -n "$CLOUDFLARE_API_TOKEN" ] && [ -n "$CLOUDFLARE_ACCOUNT_ID" ]; then echo "ok=true" >> "$GITHUB_OUTPUT";
          else echo "::notice::Cloudflare secrets are not set; skipping deploy."; fi
      - name: Apply D1 migrations
        if: steps.gate.outputs.ok == 'true'
        run: pnpm exec wrangler d1 migrations apply DB --remote
      - name: Deploy worker
        if: steps.gate.outputs.ok == 'true'
        run: pnpm exec wrangler deploy
      - name: Smoke test
        if: steps.gate.outputs.ok == 'true'
        run: |
          sleep 10
          for p in / /vi /index.md; do
            code=$(curl -s -o /dev/null -w "%{http_code}" "$SITE_URL$p")
            echo "$p → $code"; [ "$code" = "200" ] || exit 1
          done
